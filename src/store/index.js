import { createStore } from 'vuex'

// vue2创建仓库，new Vuex.Store({})
// vue3创建仓库，createStore({})

export default createStore({
  state: {
    username: 'zs'
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
